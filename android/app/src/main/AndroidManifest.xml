<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Нужно, чтобы тихо удалять фото Telegram из общей галереи после
         пересылки на сервер (файлы создаёт Telegram, не мы). -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <!-- Поднять Telegram на пару секунд из фона, чтобы он докачал медиа
         (через прокси в фоне он сам не качает). На Android 14 + HyperOS прямой
         startActivity из фона при выключенном экране заблокирован (BAL) —
         единственный системно-санкционированный путь поднять WakeActivity
         с заблокированного/погашенного экрана — full-screen-intent. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>

    <application
        android:name=".SkillwoodApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Skillwood"
        android:allowBackup="true"
        android:usesCleartextTraffic="true">

        <activity android:name=".MainActivity"
                  android:exported="true"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".AppFilterActivity"
                  android:exported="false"
                  android:label="@string/app_filter_title"
                  android:parentActivityName=".MainActivity"/>

        <activity android:name=".WakeActivity"
                  android:exported="false"
                  android:theme="@android:style/Theme.Translucent.NoTitleBar"
                  android:excludeFromRecents="true"
                  android:noHistory="true"
                  android:launchMode="singleInstance"/>

        <service android:name=".SkillwoodListener"
                 android:exported="true"
                 android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <service android:name=".ForegroundService"
                 android:foregroundServiceType="dataSync"
                 android:exported="false"/>

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>
